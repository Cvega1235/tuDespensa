import 'package:flutter/material.dart';

class DespensaMenu extends StatelessWidget {
  const DespensaMenu({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () {},
      icon: Icon(
        Icons.menu,
      ),
    );
  }
}
